version: '3.9'

services:
  dashboard-ui:
    build: .
    container_name: dashboard-ui
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
