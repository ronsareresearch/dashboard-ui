version: "3.9"

services:
  nextjs:
    build: .
    container_name: nextjs_app
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env         # Your Next.js environment variables
    volumes:
      - .:/app       # Optional: live reload when developing
